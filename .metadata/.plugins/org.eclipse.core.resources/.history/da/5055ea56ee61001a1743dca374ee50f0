

#include<stdio.h>

struct compte{

	int nombre;
	struct Pile *prec;

};

int main(void){



struct compte*a = (struct compte*) malloc(sizeof(struct compte));
a->nombre=10;
a->Pile=NULL;

for(i=10;i==0;i--){
	a->nombre -1;
	printf(a->nombre);
}
	return 0;
}
